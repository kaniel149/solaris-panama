// ─── Marketing primitives (premium light theme — bustan-energy port) ──────────
// Kept separate from components/ui/* (CRM dark theme). Use these on the public
// marketing site only (HomePage, Servicios, Nosotros, Proyectos, landing pages).
export { Button } from './Button';
export { SectionHeader } from './SectionHeader';
