# פידבק רועי על ה-CRM — טריאז' (2026-07-13)

מקור: הערות רועי (tesler.roi@gmail.com) אחרי שימוש ראשון. נבדק מול הקוד (`CrmLeadsPage.tsx`) ומול ה-DB (ubuazgwxourbzruanxvs).

## 🔴 ממצא DB קריטי (קשור לסעיף 3 של רועי)
`SELECT status, COUNT(*)`: יש עדיין **16 cold + 10 warm + 1 hot** ב-DB — למרות מיגרציה 061 שאמורה הייתה לאחד אותם. או שהמיגרציה רצה חלקית, או שמשהו (סורק? ייבוא? webhook) ממשיך להכניס סטטוסים ישנים. **27 לידים עם סטטוס שלא קיים בפילטרים של ה-CRM** → מוסתרים מכל סינון סטטוס ומעוותים את המשפך. האקדמיה גם מלמדת קר/פושר/חם — לא תואם ל-CRM.

## טבלת ממצאים

| # | הערת רועי | ורדיקט | קובץ | תיקון |
|---|---|---|---|---|
| 1 | אין פילטר Vencidos | פיצ'ר חסר | `CrmLeadsPage.tsx:114,376` — Vencidos הוא רק KPI מחושב (`isStaleLead`), לא סטטוס | להוסיף אופציית סינון וירטואלית שמפעילה `isStaleLead()` |
| 1b | קליק על KPI בדשבורד → סינון | פיצ'ר חסר | `CrmLeadsPage.tsx:369-383`, `DashboardPage.tsx:197-219` — כרטיסים לא לחיצים | onClick → `setStatusFilter(...)` |
| 2 | אין Azuero / Las Tablas באזורים | חסר/חלקי | `CrmLeadsPage.tsx:116-119` — רשימת 10 פרובינציות hardcoded + distinct רק מ-50 לידים טעונים | לשלוף `SELECT DISTINCT zone` מה-DB + להוסיף אזורים (Azuero=אזור, Las Tablas=דיסטריקט) |
| 2c | חיפוש — "אי אפשר להקיש" | צריך репро חי | ה-input תקין (`:430-435`), אבל fetch על כל הקשה בלי debounce/ביטול (`:216`) → ייתכן איבוד פוקוס/הקפאה. בעמוד הסורק יש באג אמיתי ב-`SearchInput.tsx:29` (controlled+debounce 300ms) | debounce לחיפוש + "latest request wins"; לתקן SearchInput |
| 2d | מעבר טאב → אין תוצאות עד יציאה | באג race סביר | `fetchLeads` בלי ביטול — תשובה ישנה/ריקה דורסת חדשה (`:199`) | AbortController / טוקן בקשה אחרונה |
| 3 | אקדמיה מלמדת קר/חם/פושר | אי-התאמה + באג DB | מיגרציה 061 + הממצא למעלה | לתקן דאטה + לעדכן אקדמיה או להריץ remap שוב + לתפוס את המקור שמכניס legacy |
| 4 | ליד לא כשיר — סגירה בקליק + הודעה | פיצ'ר חסר | יש `not_a_lead` וסטטוס בלבד; `enqueueWhatsAppMessage` קיים ב-`leadService.ts:223` אבל לא מחובר ל-UI | כפתור "Descartar" = סטטוס + תבנית וואטסאפ מנומסת |
| 5 | NOTA — שורה אחת, נשאר "Sin notas" | באג חלקי | `input` במקום `textarea` (`:1076`); `handleAddNote` (`:307`) בלי try/catch — כשל RLS נבלע בשקט; author='Admin' קבוע | textarea + טיפול שגיאות + toast + משתמש אמיתי |
| 6 | אין סטטוס Seguimiento; ליד עם מעקב נשאר NUEVO | פער מוצרי | "Programar seguimiento" רק מוסיף `lead_events`, לא נוגע בסטטוס (`:267-283`) | אוטו-קידום new→contacted ביצירת מעקב + תצוגת "במעקב" (badge/פילטר לפי events) |
| 6b | תת-סטטוסים ל-CONTACTADO (T+48, T+שבוע) + צבעים | הצעה מוצרית | — | לתכנן: follow_up_due על הליד + צביעה לפי זמן שעבר |

## סדר עבודה מוצע
- **P0 (באגים):** race בטעינת לידים (2d) · NOTA שקט-נכשל (5) · דאטה legacy cold/warm/hot (3)
- **P1 (זריזים):** פילטר Vencidos · KPI לחיצים · אזורים מה-DB · textarea לנוטות
- **P2 (מוצר):** Seguimiento flow (6) · Descartar+הודעה (4) · תת-סטטוסים+צבעים (6b)

## שאלות פתוחות לרועי/קניאל
1. ליד לא כשיר — לשלוח הודעת "לא רלוונטי" אוטומטית או ידנית? (bridge וואטסאפ כרגע למטה!)
2. Seguimiento = סטטוס נפרד או מצב-משנה של Contactado?
3. אילו אזורים בדיוק צריכים להופיע? (Azuero כאזור-על + דיסטריקטים?)
